# Dog remote UI (React)

```bash
cd src/gadgetpanda/dog/ui/web
npm install
npm run build   # → ../static (served by FastAPI)
npm run dev     # Vite on :5173, proxies /ws → :8766
```

Run backend:

```bash
gadgetpanda dog ui --demo
```
